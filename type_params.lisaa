
fn add3<T : Add>(T a, T b, T c) -> T {
    return a.add(b) + c;
}

trait Add = method add(Self) -> Self;

//fn add3<T : Add>(T a, T b, T c) -> T {
//    return a + b + c;
//}

fn main(){
    add3(1, 2, 3).toString().println();
}