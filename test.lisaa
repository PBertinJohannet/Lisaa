
import string

// to find root refs
// find local variables with pointers.

fn main(){
    String a = "oku";
    for (num i = 0; i<22; i= i+1;){
        if (i % 10){
            a = a  + "hi"; // add code here to say that the ref to a goes out and the ref to hi goes in.
        }
        ("a : " + a + "\n").print();
    }
}
